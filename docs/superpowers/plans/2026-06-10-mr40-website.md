# MR 40주년 사이트 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GitHub Pages에서 동작하는 MR 40주년 기념 정적 사이트 7페이지 + 사진 파이프라인을 완성한다.

**Architecture:** 빌드 없는 순수 정적 멀티페이지(HTML + Tailwind CDN + Vanilla JS). 동적 데이터는 구글시트 웹게시 CSV를 fetch, 갤러리는 로컬 Python 스크립트가 생성한 JSON + 썸네일을 사용. 스펙: `docs/superpowers/specs/2026-06-10-mr40-website-design.md`

**Tech Stack:** HTML5, Tailwind CSS(CDN), Vanilla JS, Python 3 + Pillow(사진 파이프라인), GitHub Pages, Google Sheets CSV, Cloudflare R2, YouTube embed

**제약:** 쓰기는 `40th_website/` 내부만. 원본 자료(`cd files/`, `Donated materials/`, `GoogleDrive_Backup/`, `Website_Server_Backup/`)는 읽기 전용.

---

## 파일 구조

```
40th_website/
├── index.html              메인 (히어로, D-day, 섹션 카드)
├── timeline.html           연혁 (모던→레트로 스크롤 연출)
├── members.html            주소록 (코드 게이트 + 검색)
├── gallery.html            사진관 (사진 그리드 + 영상 탭)
├── magazine.html           회지 아카이브
├── guestbook.html          방명록
├── event.html              행사 안내
├── config.js               시트 URL·폼 URL·R2 URL·코드 해시 (전 페이지 공유)
├── assets/
│   ├── style.css           공통 커스텀 CSS (Tailwind 보완: 테마 변수, 레트로 구간)
│   └── common.js           공통 JS (네비 주입, CSV 파서, 캐시 fetch, 토스트)
├── data/
│   ├── timeline.json       연혁 항목 (history 테이블 추출 + 보강)
│   ├── gallery.json        사진 메타 (스크립트 생성)
│   └── videos.json         YouTube 영상 목록 (수동 관리)
├── thumbs/                 갤러리 썸네일 WebP (스크립트 생성)
├── pdfs/                   회지 압축본 PDF
├── scripts/
│   ├── build_gallery.py    사진 → 썸네일 WebP + gallery.json
│   ├── extract_timeline.py SQL 덤프 → timeline.json 초안
│   └── test_build_gallery.py
└── README.md               운영·인수인계 가이드
```

설계 원칙: 페이지 공통 로직은 전부 `common.js` 한 곳에. 페이지별 JS는 해당 HTML 안에 인라인(파일 수 최소화, 인수인계 단순화). 데이터는 전부 `data/*.json` 또는 시트 CSV — HTML에 콘텐츠 하드코딩 금지(타임라인·갤러리·영상).

## 디자인 시스템 (전 페이지 공통)

- 배경: `#0b1530` → `#15275c` 그라데이션 다크. 포인트: `#5b8cff` (블루), 텍스트: `#fff`/`#7d8db5`
- 레트로 구간(타임라인 1980~90년대): 배경 `#0a0e14`, 포인트 `#2dd4a7`(터미널 그린), 모노스페이스
- 폰트: Pretendard(CDN), 레트로 구간 'Courier New'/D2Coding
- 네비: 모든 페이지 상단 고정 — 로고 "MR 40" + 7개 메뉴 (모바일은 햄버거). `common.js`가 주입
- CSS 변수로 테마 정의 (`--bg`, `--accent` 등) → 레트로 구간은 변수 오버라이드

---

### Task 1: 프로젝트 뼈대 + 공통 자산

**Files:** Create: `config.js`, `assets/style.css`, `assets/common.js`, `data/videos.json`(빈 배열), `index.html`(최소 뼈대)

- [ ] **Step 1: config.js 작성**

```js
// MR 40주년 사이트 설정 — 운영자는 이 파일만 수정하면 됩니다.
const CONFIG = {
  // 구글시트 '웹에 게시' CSV 주소 (미발급 시 "" — 페이지에 안내 표시됨)
  MEMBERS_CSV_URL: "",
  GUESTBOOK_CSV_URL: "",
  // 구글폼 주소
  MEMBERS_FORM_URL: "",
  GUESTBOOK_FORM_URL: "",
  EVENT_FORM_URL: "",
  // Cloudflare R2 공개 버킷 베이스 URL (예: https://pub-xxx.r2.dev)
  R2_BASE_URL: "",
  // 주소록 접속 코드의 SHA-256 해시 (코드 변경: 새 코드를 해시해서 교체)
  MEMBERS_CODE_HASH: "",
  // 행사 정보
  EVENT_DATE: "2026-11-28T17:00:00+09:00",
  EVENT_PLACE: "미정",
};
```

- [ ] **Step 2: assets/common.js 작성** — 네비 주입, CSV 파서, 캐시 fetch, 토스트

```js
// 공통 유틸 — 모든 페이지에서 <script src="assets/common.js"> 로 로드
// [1] 상단 네비게이션 주입
function injectNav(active) {
  const items = [
    ["index.html", "홈"], ["timeline.html", "연혁"], ["members.html", "주소록"],
    ["gallery.html", "사진관"], ["magazine.html", "회지"], ["guestbook.html", "방명록"],
    ["event.html", "행사안내"],
  ];
  const links = items.map(([href, label]) =>
    `<a href="${href}" class="nav-link${href === active ? " active" : ""}">${label}</a>`).join("");
  document.body.insertAdjacentHTML("afterbegin", `
    <nav class="topnav">
      <a href="index.html" class="logo">MR<span>40</span></a>
      <button class="nav-toggle" onclick="this.parentElement.classList.toggle('open')">☰</button>
      <div class="nav-links">${links}</div>
    </nav>`);
}

// [2] CSV 한 줄 파서 (따옴표 내 쉼표 처리 — CBSH 패턴)
function parseCsvLine(line) {
  return line.split(/,(?=(?:(?:[^"]*"){2})*[^"]*$)/)
             .map((c) => c.trim().replace(/^"|"$/g, "").replace(/""/g, '"'));
}

// [3] 시트 CSV 로드: localStorage 캐시 우선 표시 + 백그라운드 갱신
//     render(rows)는 헤더 제외 행 배열(각 행은 문자열 배열)을 받는다.
async function loadSheet(url, cacheKey, render, onFail) {
  const cached = localStorage.getItem(cacheKey);
  if (cached) { try { render(JSON.parse(cached)); } catch (e) {} }
  if (!url) { if (!cached && onFail) onFail("not-configured"); return; }
  try {
    const res = await fetch(url + "&t=" + Date.now());
    if (!res.ok) throw new Error(res.status);
    const rows = (await res.text()).split("\n").slice(1)
      .map(parseCsvLine).filter((r) => r.length > 1 && r[0]);
    localStorage.setItem(cacheKey, JSON.stringify(rows));
    render(rows);
  } catch (e) { if (!cached && onFail) onFail("fetch-error"); }
}

// [4] 토스트
function showToast(msg) {
  let t = document.getElementById("toast");
  if (!t) { t = document.createElement("div"); t.id = "toast"; document.body.appendChild(t); }
  t.innerText = msg; t.className = "show";
  setTimeout(() => (t.className = ""), 2500);
}

// [5] SHA-256 해시 (주소록 접속 코드 검증용)
async function sha256(text) {
  const buf = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(text));
  return [...new Uint8Array(buf)].map((b) => b.toString(16).padStart(2, "0")).join("");
}
```

- [ ] **Step 3: assets/style.css 작성** — CSS 변수 테마(다크블루/레트로), topnav, 토스트, 카드 공통 스타일. 모바일 우선
- [ ] **Step 4: index.html 최소 뼈대** — head(메타/Tailwind/Pretendard/config/common), `injectNav("index.html")`, "MR 40 — 준비 중" 텍스트만
- [ ] **Step 5: 로컬 확인** — `python3 -m http.server 8000` 후 브라우저에서 네비 렌더 확인
- [ ] **Step 6: Commit** `feat: 프로젝트 뼈대와 공통 자산`

### Task 2: 메인 페이지 (index.html)

**Files:** Modify: `index.html`

- [ ] **Step 1: 히어로 섹션** — "로봇과 함께한 마흔 해" + "KAIST MICRO ROBOT · 1986→2026", 그라데이션 배경, 스크롤 유도
- [ ] **Step 2: D-day 카운터** — `CONFIG.EVENT_DATE` 기준 일/시/분 실시간 표시, 행사 후엔 "함께해 주셔서 감사합니다"
- [ ] **Step 3: 섹션 입구 카드 6개** — 연혁/주소록/사진관/회지/방명록/행사안내, 각 카드에 한 줄 설명
- [ ] **Step 4: 모바일 확인 후 Commit** `feat: 메인 페이지`

### Task 3: 주소록 (members.html)

**Files:** Modify: `members.html`(생성)

- [ ] **Step 1: 코드 게이트** — 진입 시 접속 코드 입력 화면. 핵심 로직:

```js
async function checkCode() {
  const code = document.getElementById("code-input").value.trim();
  if (!CONFIG.MEMBERS_CODE_HASH) { showToast("접속 코드가 아직 설정되지 않았습니다."); return; }
  if ((await sha256(code)) === CONFIG.MEMBERS_CODE_HASH) {
    sessionStorage.setItem("mr40_auth", "1");
    showDirectory();
  } else { showToast("코드가 올바르지 않습니다."); }
}
// 페이지 로드 시: sessionStorage.getItem("mr40_auth") === "1" 이면 바로 showDirectory()
```

- [ ] **Step 2: 명부 렌더** — `loadSheet(CONFIG.MEMBERS_CSV_URL, "mr40_members", render, fail)`. 시트 열 순서: 이름, 기수(입학년도), 소속, 이메일, 전화, 관심사, 한마디. 카드형 그리드, CBSH 패턴(관심사 태그 클릭 필터, 번호/메일 복사 버튼, 소속 컬러 배지) 재사용
- [ ] **Step 3: 검색** — 이름·기수·소속·관심사 통합 검색 input (CBSH 패턴)
- [ ] **Step 4: "정보 등록/수정" FAB** → `CONFIG.MEMBERS_FORM_URL`
- [ ] **Step 5: 빈 URL/실패 시 안내 화면 확인, Commit** `feat: 주소록`

### Task 4: 방명록 (guestbook.html)

**Files:** Modify: `guestbook.html`(생성)

- [ ] **Step 1: 헤더 + "축하 한마디 남기기" 버튼**(`GUESTBOOK_FORM_URL` 새 탭)
- [ ] **Step 2: 메시지 카드 렌더** — `loadSheet(..., "mr40_guestbook", ...)`. 시트 열: 타임스탬프, 이름, 기수, 메시지. 최신순, 기수 배지
- [ ] **Step 3: Commit** `feat: 방명록`

### Task 5: 사진 파이프라인 (scripts/build_gallery.py)

**Files:** Create: `scripts/build_gallery.py`, `scripts/test_build_gallery.py`, `scripts/curation.txt`(대상 목록)

- [ ] **Step 1: 실패하는 테스트 작성** — `test_build_gallery.py` (표준 unittest):

```python
import unittest, tempfile, json
from pathlib import Path
from PIL import Image
from build_gallery import process_image, guess_year

class TestBuildGallery(unittest.TestCase):
    def test_guess_year_from_path(self):
        self.assertEqual(guess_year(Path("cd files/2005 MR/photo.jpg")), 2005)
        self.assertEqual(guess_year(Path("cd files/1998 활동사진/a.jpg")), 1998)
        self.assertIsNone(guess_year(Path("misc/photo.jpg")))

    def test_process_image_creates_thumb_and_meta(self):
        with tempfile.TemporaryDirectory() as td:
            src = Path(td) / "2003 MR" / "img.jpg"; src.parent.mkdir()
            Image.new("RGB", (2000, 1500), "red").save(src, quality=95)
            out = Path(td) / "thumbs"
            meta = process_image(src, out, base=Path(td))
            self.assertTrue((out / meta["thumb"]).exists())
            self.assertEqual(meta["year"], 2003)
            self.assertLess((out / meta["thumb"]).stat().st_size, 80_000)  # 썸네일 80KB 미만

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실행해 실패 확인** — `cd scripts && python3 -m unittest test_build_gallery -v` → ImportError 예상
- [ ] **Step 3: build_gallery.py 구현**:

```python
"""원본 사진 → 갤러리 썸네일(WebP) + data/gallery.json 생성.
사용법: python3 build_gallery.py <목록파일> [--full-out DIR]
  목록파일: 한 줄에 하나씩, MR_ws 기준 상대경로 (curation.txt)
  --full-out: R2 업로드용 원본급(장변 1920px) WebP도 생성
원본은 절대 수정하지 않는다 (읽기 전용)."""
import json, re, sys, hashlib
from pathlib import Path
from PIL import Image, ImageOps

WS = Path(__file__).resolve().parents[2]      # MR_ws
SITE = Path(__file__).resolve().parents[1]    # 40th_website
THUMB_LONG, FULL_LONG, THUMB_Q, FULL_Q = 480, 1920, 70, 82

def guess_year(path):
    for part in path.parts:
        m = re.search(r"(19[8-9]\d|20[0-2]\d)", part)
        if m: return int(m.group(1))
    return None

def process_image(src, thumb_dir, base, full_dir=None):
    img = ImageOps.exif_transpose(Image.open(src)).convert("RGB")
    key = hashlib.md5(str(src.relative_to(base)).encode()).hexdigest()[:12]
    thumb_dir.mkdir(parents=True, exist_ok=True)
    t = img.copy(); t.thumbnail((THUMB_LONG, THUMB_LONG))
    t.save(thumb_dir / f"{key}.webp", "WEBP", quality=THUMB_Q)
    if full_dir:
        full_dir.mkdir(parents=True, exist_ok=True)
        f = img.copy(); f.thumbnail((FULL_LONG, FULL_LONG))
        f.save(full_dir / f"{key}.webp", "WEBP", quality=FULL_Q)
    return {"thumb": f"{key}.webp", "year": guess_year(src.relative_to(base)),
            "src": str(src.relative_to(base))}

def main():
    listfile = Path(sys.argv[1])
    full_out = Path(sys.argv[sys.argv.index("--full-out") + 1]) if "--full-out" in sys.argv else None
    items, errors = [], []
    for line in listfile.read_text().splitlines():
        line = line.strip()
        if not line or line.startswith("#"): continue
        try:
            items.append(process_image(WS / line, SITE / "thumbs", WS, full_out))
        except Exception as e:
            errors.append(f"{line}: {e}")
    items.sort(key=lambda x: (x["year"] or 0, x["src"]))
    (SITE / "data").mkdir(exist_ok=True)
    (SITE / "data" / "gallery.json").write_text(
        json.dumps(items, ensure_ascii=False, indent=1))
    print(f"완료: {len(items)}장, 실패 {len(errors)}건")
    for e in errors: print("  !", e)

if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 테스트 통과 확인** — `python3 -m unittest test_build_gallery -v` → OK
- [ ] **Step 5: 샘플 실행** — 원본 폴더에서 수십 장 경로로 `curation.txt` 샘플 작성 → 실행 → `thumbs/`·`gallery.json` 확인 (원본 무변경 확인)
- [ ] **Step 6: Commit** `feat: 사진 파이프라인 (썸네일+메타 생성)`

### Task 6: 사진관 (gallery.html)

**Files:** Modify: `gallery.html`(생성)

- [ ] **Step 1: 탭 UI** — [사진 | 영상]
- [ ] **Step 2: 사진 탭** — `data/gallery.json` fetch → 연도(또는 연대) 필터 칩 + 썸네일 그리드. `<img loading="lazy">` 필수(수천 장 대비)
- [ ] **Step 3: 라이트박스** — 클릭 시 전체화면, `CONFIG.R2_BASE_URL`이 있으면 원본급(`R2_BASE_URL/full/{key}.webp`), 없으면 썸네일 확대. ←/→ 키·스와이프 탐색, 연도 캡션
- [ ] **Step 4: 영상 탭** — `data/videos.json`(`[{"id":"yt-id","title":"","year":1992,"desc":""}]`) → 연도순 카드, 클릭 시 YouTube iframe 로드(초기엔 썸네일만 — 성능)
- [ ] **Step 5: 모바일 확인, Commit** `feat: 사진관`

### Task 7: 타임라인 데이터 추출 (scripts/extract_timeline.py)

**Files:** Create: `scripts/extract_timeline.py`, `data/timeline.json`

- [ ] **Step 1: 추출 스크립트** — `Website_Server_Backup/mr_homepage_dump.sql`(읽기만)에서 `history` 테이블 INSERT 구문을 정규식 파싱 → `[{"date":"...","text":"..."}]` 초안 JSON 출력. mysql 불필요:

```python
"""기존 홈페이지 SQL 덤프에서 history 테이블을 timeline.json 초안으로 추출.
사용법: python3 extract_timeline.py  (원본 SQL은 읽기만 한다)"""
import json, re
from pathlib import Path

WS = Path(__file__).resolve().parents[2]
SQL = WS / "Website_Server_Backup" / "mr_homepage_dump.sql"
OUT = Path(__file__).resolve().parents[1] / "data" / "timeline.json"

def parse_rows(sql_text):
    m = re.search(r"INSERT INTO `history` VALUES (.+?);\n", sql_text, re.S)
    if not m: return []
    rows = re.findall(r"\((\d+),'((?:[^'\\]|\\.)*)','((?:[^'\\]|\\.)*)'\)", m.group(1))
    unesc = lambda s: s.replace("\\'", "'").replace('\\"', '"').replace("\\n", "\n")
    return [{"date": unesc(d), "text": unesc(t)} for _, d, t in rows]

if __name__ == "__main__":
    items = parse_rows(SQL.read_text(encoding="utf-8", errors="replace"))
    OUT.write_text(json.dumps(items, ensure_ascii=False, indent=1))
    print(f"{len(items)}건 → {OUT}")
```

- [ ] **Step 2: 실행·검수** — 111건 추출 확인, 깨진 인코딩·중복 수동 정리. 스키마 확장: `{"year":1986,"date":"1986.3","title":"창립","text":"...","media":{"thumb":"...","yt":"..."}}` 형태로 정리(초안 기반 수동 편집)
- [ ] **Step 3: Commit** `feat: 타임라인 데이터 추출`

### Task 8: 연혁 타임라인 (timeline.html)

**Files:** Modify: `timeline.html`(생성)

- [ ] **Step 1: 세로 타임라인 렌더** — `timeline.json` fetch → 연대 구분선 + 항목 카드(연도, 제목, 본문, 미디어)
- [ ] **Step 2: 레트로 전환 연출** — 2026→1986 역순 스크롤. IntersectionObserver로 현재 연대 감지 → 1990년대 이전 구간 진입 시 `<body data-era="retro">` 토글 → CSS 변수 전환(다크블루→터미널 그린, 폰트 모노스페이스). 부드러운 transition
- [ ] **Step 3: 모바일 확인, Commit** `feat: 연혁 타임라인 (레트로 연출)`

### Task 9: 회지 아카이브 (magazine.html)

**Files:** Modify: `magazine.html`(생성), Create: `pdfs/`(압축 PDF), `data/magazines.json`

- [ ] **Step 1: PDF 준비** — Donated materials의 재정렬·분리본(1~7MB)을 `pdfs/`로 복사(원본 무수정). 70MB급 원본은 R2 대상으로 보류. `data/magazines.json`: `[{"title":"큰 마음 작은 이야기","year":1996,"file":"pdfs/1996_큰_마음_작은_이야기.pdf","cover":"thumbs/..." }]`
- [ ] **Step 2: 표지 그리드** — 표지 이미지는 PDF 1쪽을 썸네일로 추출(Pillow+pdf 변환 또는 스크린샷). 클릭 → PDF 새 탭
- [ ] **Step 3: Commit** `feat: 회지 아카이브`

### Task 10: 행사 안내 (event.html)

**Files:** Modify: `event.html`(생성)

- [ ] **Step 1: 행사 정보 섹션** — 일시(11/28)·장소(`CONFIG.EVENT_PLACE`)·프로그램 (확정 전 항목은 "추후 공지" 표시 — 데이터는 페이지 상단 JS 상수로 분리해 수정 쉽게)
- [ ] **Step 2: 참가 신청 버튼** — `EVENT_FORM_URL`, D-day 카운터 재사용
- [ ] **Step 3: Commit** `feat: 행사 안내`

### Task 11: README 운영 가이드 + 마감 점검

**Files:** Create: `README.md`

- [ ] **Step 1: README 작성** — CBSH readme 스타일: 운영 원리 3줄 요약 / 매년 할 일 / 구글폼·시트 연결법 / 접속 코드 변경법(sha256 생성 명령 포함) / 사진 추가법(curation.txt + build_gallery.py) / 영상 추가법(videos.json) / 문제 해결 FAQ / R2·YouTube 계정 관리 위치
- [ ] **Step 2: 전 페이지 점검** — 로컬 서버로 7페이지 모바일 폭(375px)·데스크톱 확인, 콘솔 에러 0 확인
- [ ] **Step 3: Lighthouse 성능 확인** — gallery.html lazy load 동작 확인
- [ ] **Step 4: Commit** `docs: 운영 가이드`

### Task 12 (외부 의존, 사용자와 함께): 배포·연동

- [ ] GitHub 리포 생성·push·Pages 활성화 (계정/리포명 사용자 확정)
- [ ] 구글폼 3종 + 시트 웹게시 → `config.js` URL 기입
- [ ] 접속 코드 결정 → 해시 기입
- [ ] Cloudflare R2 버킷 생성 → full WebP 업로드 → `R2_BASE_URL` 기입
- [ ] YouTube 업로드(사용자 계정) → `videos.json` 작성
- [ ] 사진 큐레이션 확정 → 전량 파이프라인 실행

---

## Self-Review 결과

- 스펙 커버리지: 7페이지(T1~T4, T6, T8~T10), 파이프라인(T5, T7), 에러 처리(common.js onFail·캐시), 운영 가이드(T11), 미결 사항(T12) — 전부 매핑됨
- 타입 일관성: `loadSheet(url, cacheKey, render, onFail)` 시그니처와 T3/T4 사용처 일치, `process_image(src, thumb_dir, base, full_dir)` 와 테스트 호출 일치 (테스트는 키워드 인자 `base=` 사용)
- 외부 계정 의존 작업은 T12로 분리 — T1~T11은 빈 URL 상태로도 동작·검증 가능 (onFail 안내 화면)
